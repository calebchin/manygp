experiment:
  name: cifar10_due_ms
  smoke_test: false

data:
  root: ./data
  batch_size: 128
  num_workers: 4

model:
  sngp: false
  latent_dim: 64
  num_classes: 10
  widen_factor: 10
  embedding_dim: 128
  inducing_pool_size: 1000
  num_inducing_pts: 10
  num_mc_samples: 10
  num_output: 10
  per_feature: false

training:
  num_epochs: 100
  initial_lr: 0.1
  milestones: [30, 60, 80]
  beta_reg: 0.05
  ms_weight: 1.0
  ms_alpha: 2.0
  ms_beta: 50.0
  ms_base: 0.5
  ms_eps: 0.1

outputs:
  checkpoint_path: ./checkpoints_due/cifar10_ms.pt

wandb:
  enabled: true
  project: due_exp_fix
  entity: sta414manygp
  run_name: due_ms    # null = auto-generated by W&B
